{
  "version": "v1",
  "hooks": [
    {
      "name": "Credential Expiry Warning",
      "trigger": "SessionStart",
      "description": "At session start, warn if a credential this repo depends on is near expiry, already expired, or missing from the keychain. Silent when healthy. Exits 0 always so it can never block a session. Reads .kiro/credentials.json; never reads or prints secret values.",
      "action": {
        "type": "command",
        "command": "python3 \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/.kiro/scripts/check-credential-expiry.py\" 2>/dev/null || true",
        "timeout": 30
      }
    }
  ]
}